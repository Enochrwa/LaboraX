name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy (placeholder)
    runs-on: ubuntu-latest
    if: github.repository == 'Enochrwa/LaboraX'
    steps:
      - uses: actions/checkout@v4
      - name: Deploy step (to be configured)
        run: |
          echo "Wire this job to Render/Fly.io (backend) and Vercel (frontend) deploy hooks."
          echo "Intentionally a no-op placeholder until hosting is provisioned - see docs/HLD.md."
