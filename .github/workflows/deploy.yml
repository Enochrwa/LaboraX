name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy (placeholder)
    runs-on: ubuntu-latest
    if: github.repository == 'Enochrwa/LaboraX'
    steps:
      - uses: actions/checkout@v7
      - name: Deploy step (to be configured)
        run: |
          echo "Backend config-as-code is ready: infra/render.yaml (Render) or infra/fly.toml (Fly.io)."
          echo "Frontend config-as-code is ready: frontend/vercel.json."
          echo "Wire this job to whichever deploy hook/CLI once hosting accounts exist - see docs/HLD.md."
          echo "Intentionally a no-op placeholder until hosting is provisioned; no deploy credentials exist yet."
